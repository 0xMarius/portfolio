<span class="blinker">|</span>

<style>
    .blinker {
        color: #121212;
        animation: blink 1.5s infinite;
    }
    @keyframes blink {
        0%{ opacity: 100; }
        50%{ opacity: 0; }
        100%{ opacity: 100; }

    }
</style>
